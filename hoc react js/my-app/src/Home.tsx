import React from "react";
import SwitchButton from "./SwitchButton";

export default class Home extends React.Component {
  render() {
    return (
      <div>

        <SwitchButton stateLight={1 name = "loc"} />
      </div>
    );
  }
}
